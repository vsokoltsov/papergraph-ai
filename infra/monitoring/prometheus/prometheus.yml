global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  # Add the app metrics endpoint here once the API exposes /metrics.
  # Example:
  # - job_name: papergraph-api
  #   static_configs:
  #     - targets:
  #         - api:8000
